# Legacy Specification Drafts

This directory contains early specification experiments.

The authoritative v0.2 documents are located in `docs/`.

In particular, Manifest + Modules is not mandatory for the first real implementation. Use `docs/THEME_PACK_SPEC.md` and `docs/COMPONENT_SKIN_CONTRACT.md` instead.

中文说明：
这里的旧规范暂时保留用于追溯，但不能继续作为开发标准。
